{
  "name": "product-preview-card-component",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dest Product-preview-card-component --add"
  },
  "devDependencies": {
    "sass-embedded": "^1.104.0",
    "vite": "^8.2.2"
  }
}
